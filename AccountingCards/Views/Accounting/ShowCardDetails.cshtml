@model AccountingViewModel

@{
    ViewBag.Title = "title";
    Layout = "_Layout";
}

<div id="app">
    <div>
        <h2>{{ card.name }}</h2>
        <div class="row mt-4">
            <table class="table">
                <thead class="thead-light">
                <tr>
                    <th scope="col">Order</th>
                    <th scope="col">Date</th>
                    <th scope="col" colspan="2">Details</th>
                    <th scope="col">Price</th>
                    <th scope="col">Card</th>
                </tr>
                </thead>
                <tbody>
                    <tr v-for="detail in details">
                        <td name="order" class="align-middle detail-order text-center">{{ detail.order }}</td>
                        <td class="align-middle">{{ detail.date }}</td>
                        <td colspan="2" class="align-middle">{{ detail.info }}</td>
                        <td class="align-middle">{{ detail.price }}</td>
                        <td class="align-middle">{{ detail.card.name }}</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
</div>



@section Scripts
{
    <script src="https://unpkg.com/vue@next"></script>

    <script>
    
    const app = Vue.createApp({
        data(){
            return {
                card: @Json.Serialize(Model.Cards[0]),
                details: @Json.Serialize(Model.Details),
            }
        }
    });
    app.mount('#app');
    
    </script>
}
